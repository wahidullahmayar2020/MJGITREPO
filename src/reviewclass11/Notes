Collections FrameWork:
collection of classes and interfaces that can store and manipulate data using different
built-in data structures like arrays linkedLists and sets.

Wrapper class:
1)are object Equivalent of primitive types
2) Collections can not work with primitive data types they can only work with objects
3) Wrapper class provide built-in methods to convert the data types and some useful Constants


ArrayList:
it implements the List interface
 1) It can hold duplicate values not issues
 2) Insertion order is maintained
 3) Dynamic in nature can adjust the size at run time automatically
 4) Underlying data structure is Arrays
Methods in ArrayList Class:
    add()----adds the element in natural sequence
    add()--- we can specify the index
    get(int index) ---- returns the element specified index
    remove(int index) -- removes the elements at specified index
    remove(Object)  --object will be removed
